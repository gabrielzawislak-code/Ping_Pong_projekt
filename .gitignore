*.log
*.bin
*.exe

/runs/
/usage_statistics_webtalk.*
*.jou
*.log
.Xil/

# Vivado / DVT generated project data - not source, must be regenerated
# locally via tools/generate_bitstream.sh / tools/run_simulation.sh.
.dvt/
xsim.dir/
xvlog.pb
fpga/build/
sim/build/
fpga/dfx_runtime.txt
tools/build/

# Simulation frame dumps - regenerate with tools/run_simulation.sh, do not
# commit. NOTE: results/*.bit is intentionally NOT ignored - the course
# requirements (10.1.3) require the final bitstream to be committed in
# results/ for grading.
results/*.tif
