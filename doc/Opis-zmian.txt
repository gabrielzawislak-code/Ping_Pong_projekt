Ping-Pong na Basys3 - opis zmian architektonicznych
Mateusz Zybura, Gabriel Zawiœlak

1. PROBLEMY PRZED ZMIAN¥:
a) Projekt by³ podzielony na dwa osobne bitstreamy (ga³êzie master_new i  player_2_new), z których tylko jeden (master_new) generowa³ obraz VGA i prowadzi³ rozgrywkê - druga p³ytka by³a zredukowana do kontrolera przycisków bez w³asnego ekranu.
b) Komunikacja UART by³a jednokierunkowa: p³ytka master_new mia³a tylko wejœcie RX, bez nadajnika TX.
c) Dwie ga³êzie w repozytorium oznacza³y, ¿e po sklonowaniu i zbudowaniu ga³êzi domyœlnej powstawa³a tylko jedna z dwóch konfiguracji.
2. WPROWADZONA ZMIANA ARCHITEKTURY
Zamiast dwóch ró¿nych projektów - jeden, identyczny bitstream wgrywany na OBIE p³ytki Basys3. Rolê (HOST / Gracz 1 albo CLIENT / Gracz 2) wybiera fizyczny prze³¹cznik  (SW0) sw_role, odczytywany w locie:
- HOST (sw_role = 0): jedyna p³ytka licz¹ca fizykê pi³ki i wynik (modu³ ball_pos). Wysy³a do drugiej p³ytki pe³ny stan gry. 
- CLIENT (sw_role = 1): nie liczy pi³ki ani wyniku, mirroruje je z ramki otrzymanej od HOSTa.
Obie p³ytki, niezale¿nie od roli:
  - maj¹ aktywne wyjœcie VGA i renderuj¹ obraz lokalnie (w³asny monitor,
    w³asna kopia rozgrywki),
  - licz¹ lokalnie WY£¥CZNIE w³asn¹ paletkê (modu³ paddle_mover) na podstawie w³asnych przycisków - zero opóŸnienia w sterowaniu,
  - prowadz¹ lokalny automat stanu gry (game_fsm) zsynchronizowany z drug¹ p³ytk¹ przez wymianê stanu (IDLE/READY/PLAYING/END) - obie strony musz¹ wcisn¹æ start (btnC), by rozpocz¹æ rozgrywkê.
Komunikacja UART jest pe³nodupleksowa, dwiema oddzielnymi ramkami w obu kierunkach jednoczeœnie:
  HOST -> CLIENT (12 B, co ~1/60 s): pozycje obu paletek, pozycja pi³ki, wynik obu graczy, stan gry.
  CLIENT -> HOST (4 B, co ~1/60 s): w³asna (lokalnie policzona) pozycja paletki CLIENTa oraz jego lokalny stan gry.
Piny: JC[0] = RX, JXADC[0] = TX - identycznie na obu p³ytkach. P³ytki ³¹czy siê na krzy¿ (TX jednej do RX drugiej) plus wspólna masa (GND).

3. ZMIANY W KODZIE (repozytorium scalone do jednej ga³êzi)
Nowe/zmienione modu³y:
  - rtl/game/paddle_mover.sv (nowy) - pozycja jednej, lokalnej paletki; zastêpuje obie wczeœniejsze wersje paddle_pos.sv.
  - rtl/game/game_fsm.sv - wersja z wejœciem peer_state, u¿ywana teraz na obu p³ytkach (wczeœniej istnia³a, ale nieu¿ywana).
  - rtl/uart/send_state_frame.sv / receive_state_frame.sv (przemianowane z send_bytes.sv / receive_bytes.sv, logika bez zmian) - kierunek HOST -> CLIENT.
  - rtl/uart/send_paddle_frame.sv / receive_paddle_frame.sv (nowe) - kierunek CLIENT -> HOST.
  - rtl/top_vga.sv oraz fpga/rtl/top_vga_basys3.sv - przepisane wokó³ sygna³u roli is_host (z podwójnie synchronizowanego prze³¹cznika sw_role), multiplekseru danych paletka/pi³ka/wynik oraz multipleksera ramek TX/RX.
  - fpga/constraints/top_vga_basys3.xdc - dodany pin sw_role (SW0), w³¹czony pin TX JXADC[0] (J3); VGA i btnC aktywne na sta³e.
  - Zaktualizowane pliki .prj i testbenche w sim/ pod nowe/przemianowane modu³y i porty.
Repozytorium zamienione na jeden katalog projektu, jedna ga³¹Ÿ g³ówna (main) zamiast 
master_new / player_2_new.

4. WERYFIKACJA
  - synteza i implementacja zakoñczone bez b³êdów, bitstream wygenerowany poprawnie,
  - timing siê zgadza (dodatni WNS, 0 failing endpoints),
  - Wszystkie ostrze¿enia (warningi) zg³oszone przez Vivado na etapach syntezy, implementacji oraz DRC zosta³y przeanalizowane. S¹ to standardowe, nieszkodliwe komunikaty narzêdzia oraz advisory dotycz¹ce istniej¹cych wczeœniej modu³ów, które nie maj¹ ¿adnego wp³ywu na dzia³anie wprowadzonych zmian ani na poprawnoœæ uk³adu.

Napotkane problemy przy tej wersji projektu:
wystêpuje problem z komunikacj¹ po stronie HOST -> CLIENT

5. ZNANY, NIEROZWI¥ZANY PROBLEM
Potwierdzona poprawnoœæ i weryfikacja na sprzêcie:
Uzgadnianie stanu gry (IDLE/READY/PLAYING/END): Stabilna komunikacja dwukierunkowa miêdzy p³ytkami. Wêze³ HOST: Pe³ny przebieg rozgrywki dzia³a prawid³owo (lokalna fizyka pi³ki, sterowanie paletk¹ HOSTa oraz poprawny odbiór pozycji paletki CLIENTa w ramce 4-bajtowej). Wêze³ CLIENT: Sterowanie lokaln¹ paletk¹ dzia³a poprawnie.Dekodowanie ramek HOST -> CLIENT: Logika dekodowania 12-bajtowej ramki jest poprawna. Zweryfikowano diagnostycznie (zliczanie bajtu STOP) oraz potwierdzono przy jednorazowym odblokowaniu ³¹cza (jednoczesne wyœwietlenie poprawnego stanu 9:3 oraz pozycji paletek na obu ekranach).
Opis usterki:
Podczas ci¹g³ej pracy CLIENT nie odœwie¿a pozycji pi³ki, paletki HOSTa ani wyniku (stan na ekranie zamra¿a siê na wartoœciach pocz¹tkowych). Jednorazowe odœwie¿enie obrazu prze³¹czenie prze³¹cznika roli (sw_role) na p³ytce HOSTa w stan CLIENT i powrót do HOST, czasami powoduje przes³anie poprawnej ramki z danymi na temat paletki pi³ki i wyniku.
W rozwi¹zaniu usterki nie pomaga³y problemy z laptopem, który przy wgrywaniu bitstreama na p³ytkê Basys3 przy czêœci prób restartowa³ siê.
Wykluczone przyczyny:
    Warstwa fizyczna: Wymiana przewodów oraz testy mechaniczne gniazd Pmod nie wp³ynê³y na objawy.
    Buforowanie UART: Zwiêkszenie g³êbokoœci FIFO z 4 do 16 s³ów – brak poprawy.
    Timing / Byterate: Zmniejszenie prêdkoœci UART z ~19 200 do ~9600 baud – brak poprawy.
    Resetowanie FSM: Cykliczne resetowanie FSM nadajnika/odbiornika (co ~200 ms) pogorszy³o stabilnoœæ (ca³kowity brak aktualizacji).




































